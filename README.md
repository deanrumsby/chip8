# chip8
A chip8 interpreter written in TypeScript

Learning objectives: 
- To use TypeScript in a project
- To learn more about computer science

To run the test suite, use the command "npm test".
To make a build of the project, use "npm build"